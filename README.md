# VENDtrack
A simple webpage, that uses a python backend, basic html and css styling for the webpages, and can fetch geographical location data from an ESP32 module via WiFi which is stored in a SQLlite database.
 
